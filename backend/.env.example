# Copy to .env and adjust for your environment.
# SQLite is the default local/dev/test database (no PostGIS available).
# For production, point DATABASE_URL at a PostGIS-enabled Postgres instance,
# e.g. postgresql+psycopg://user:pass@localhost:5432/sankatsetu
DATABASE_URL=sqlite:///./sankatsetu.db
ENV=development
